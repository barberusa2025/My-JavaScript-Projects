// This is an array of colors used for selection in a user interface.
let selectionColors = ['red', 'blue'];

selectionColors[2] = 'green'; // Adding a new color to the array    

// Display the first color in the array
console.log(selectionColors[0]);

// Display the second color in the array
console.log(selectionColors[1]);

// Display the third color in the array
console.log(selectionColors[2]);

console.log(selectionColors.length); // Display the length of the array


